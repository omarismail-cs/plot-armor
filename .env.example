# Copy to .env for local unpacked dev (never commit .env).
# Or set the same values in Extension options → plot armor settings.

OPENAI_API_KEY=
TMDB_READ_ACCESS_TOKEN=

# Optional — false-positive ingest (see supabase/README.md)
FALSE_POSITIVE_INGEST_URL=
FALSE_POSITIVE_INGEST_KEY=
